#!/usr/bin/env bash
set -euo pipefail

main() {
  eza \
    -amMT \
    -L 2 \
    -F=always \
    --color=always \
    --icons=always \
    --group-directories-first \
    "$@"
}

main "$@"

# vim: set ft=sh:
