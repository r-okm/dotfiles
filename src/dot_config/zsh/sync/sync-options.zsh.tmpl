# vim: set ft=sh:

# setopt
setopt nobeep
setopt nolistbeep
setopt correct
setopt auto_cd

# cdpath
cdpath=({{ index . "cdpath" | default "" }} $cdpath)

# fpath
fpath=(
  $ZDOTDIR/fpath(N-/)
  $fpath
)
