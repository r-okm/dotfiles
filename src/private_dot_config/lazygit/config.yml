git:
  paging:
    colorArg: always
    pager: delta --features=git --paging=never
os:
  editPreset: "nvim-remote"
  copyToClipboardCmd: echo -n {{text}} | clipboard --yank
keybinding:
  files:
    commitChanges: c
    commitChangesWithEditor: C
gui:
  nerdFontsVersion: "3"
  sidePanelWidth: 0.25
