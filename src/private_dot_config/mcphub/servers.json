{
  "nativeMCPServers": [
    
  ],
  "mcpServers": {
    "terraform": {
      "command": "docker",
      "disabled": true,
      "args": [
        "run",
        "-i",
        "--rm",
        "hashicorp/terraform-mcp-server"
      ]
    }
  }
}