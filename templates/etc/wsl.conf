# ログインユーザー
[user]
default = okmt

# dnsTunneling による DNS 解決のため、WSL に resolv.conf を自動生成させる
[network]
generateResolvConf = true

# host 側に対して chmod 可能にする
[automount]
options = "metadata,umask=22"

# host 側の path を引き継がない
[interop]
appendWindowsPath = false

[boot]
# systemd を有効化
systemd = true
